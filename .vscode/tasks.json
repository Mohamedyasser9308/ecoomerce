{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Upload to GitHub",
      "type": "shell",
      "command": "git add .; git commit -m 'Update projects'; git push",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": []
    }
  ]
}